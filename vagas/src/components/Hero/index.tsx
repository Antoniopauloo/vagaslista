import { HeroContainer, HeroTitle, HeroText } from './styles';

const Hero = () => {
  return (
    <HeroContainer>
      <HeroTitle>As melhores vagas para tecnologia, design e artes visuais.</HeroTitle>
      <HeroText>
        Encontre as melhores oportunidades em empresas que valorizam o seu talento.
      </HeroText>
    </HeroContainer>
  );
};

export default Hero;