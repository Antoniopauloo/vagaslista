import { HeaderContainer, HeaderTitle } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Vagas</HeaderTitle>
    </HeaderContainer>
  );
};

export default Header;